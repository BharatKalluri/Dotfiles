#!/usr/bin/env python

import sys

# TODO: tons of validation required, make this a proper cli app

for input_from_pipe in sys.stdin:
    split_by = sys.argv[1]
    get_arr_el = sys.argv[2]
    print(input_from_pipe.split(split_by)[int(get_arr_el)].strip())
